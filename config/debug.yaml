data:
  train_dataset: sdm_bench
  eval_dataset: sdm_bench

model_kwargs:
  n_hops: 2
  n_intermediate: 4
  heads: 4

trainer_kwargs:
  max_epochs: 1
  log_every_n_steps: 10
  accelerator: "cpu"

wandb_init_kwargs:
  mode: "disabled"
